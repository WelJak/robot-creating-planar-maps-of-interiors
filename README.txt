Repository contains code used for engineering thesis.
The code allows robot to move independently inside rooms and collect 
data about obstacles that stands in its way.
LabView code receives data collected by robot and then draws a planar map which reflects actual state of the room.